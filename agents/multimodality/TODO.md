# TODO

- Add a deeper diagnosis path for `fully_async + sglang` queue / scheduler progress if that mode becomes a blocker again.
- Try a larger VLA matrix only after the 1-step Libero smoke path is stable.
- Evaluate replay bottlenecks again if future multimodal or VLA runs begin storing raw media tensors or cached dense vision embeddings.
